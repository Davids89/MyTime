import 'package:flutter/material.dart';
import 'package:my_time/screens/home.dart';

void main() => runApp(MyTime());

class MyTime extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Home();
  }
}
